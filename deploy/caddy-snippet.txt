# Caddy site block for the opencanada front door - AS DEPLOYED (2026-06-12).
# Express serves the built SPA itself in production, so this block is a pure
# reverse_proxy: additive to the live Caddyfile, reload-only, no container
# mounts touched on the shared box.
#
# Live Caddyfile: opt/<platform>-project/volumes/proxy/caddy/Caddyfile
# Reload:        docker exec <platform>-caddy caddy reload --config /etc/caddy/Caddyfile
#
# The <redacted-host> hostname is the temporary zero-DNS domain. When a real domain
# lands: point DNS at <redacted-ip>, change ONLY the site label below, reload.

<redacted-host> {
	encode zstd gzip
	header -Server
	reverse_proxy 172.18.0.1:3100
}
